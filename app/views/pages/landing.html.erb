<h1>Pages#landing</h1>

<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <!-- <th colspan="3"></th> -->
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr class="table table-light">
        <td><%= student.name %></td>
        <td><%= student.email %></td>
      
      </tr>
    <% end %>
  </tbody>
  </table>
